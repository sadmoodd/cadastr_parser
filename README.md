# PDF EGРN Parser AI Agent (Qwen3-VL)

AI-агент для автоматического извлечения данных из PDF-выписок ЕГРН с помощью модели Qwen3-VL через Hugging Face Router и сохранением результата в Excel.

Проект предназначен для обработки пачки PDF-файлов с выписками, извлечения ключевых реквизитов (кадастровый номер, адрес, площадь, собственник, аренда и т.п.) и формирования единой таблицы.

---

## Основные возможности

- Интерактивное **числовое меню** для удобного управления.
- Обработка PDF-файлов с помощью **LLM Qwen3-VL-30B-A3B-Instruct** через Hugging Face Router.
- Автоматическое:
  - преобразование PDF → изображение (первая страница);
  - отправка изображения и инструкции в модель;
  - парсинг JSON-ответа модели;
  - пост-обработка полей с помощью `REGEX_PATTERNS` из `settings.py`.
- Создание итогового **DataFrame** и сохранение в **Excel** (формат и структура как в исходном проекте).
- Краткий **текстовый отчёт** о результатах обработки (кол-во файлов, успешных/с ошибками, время, путь к Excel).
- Логирование всех шагов в файл (через `logger_cfg.py`).

---

## Структура проекта
```
cadastr_parser/
├── main.py # Главный скрипт с AI-агентом (Qwen3-VL)
├── logger_cfg.py # Настройка логирования
├── settings.py # Конфигурация проекта, пути, MESSAGES, REGEX_PATTERNS
├── table_builder.py # Формирование строк и DataFrame
├── excel_writer.py # Сохранение DataFrame в Excel с форматированием
├── requirements.txt # Список зависимостей
└── data/
├── input/ # Входные PDF-файлы
├── output/ # Выходные Excel-файлы
└── logs/ # Логи работы
```
---

## Требования

- Python 3.9+ (рекомендуется)
- Установленные системные зависимости для `pdf2image`:
  - Linux: `poppler-utils`
  - macOS: `brew install poppler`
  - Windows: установить poppler и прописать путь в PATH

---

## Установка

```
git clone <URL_вашего_репозитория>
cd cadastr_parser
```

## Установить зависимости

```
pip install -r requirements.txt
```

### Переменная окружения HF_TOKEN

Проект использует Hugging Face Router API. Токен не хранится в коде и должен быть передан через переменную окружения `HF_TOKEN`.
```
export HF_TOKEN="hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx" # Linux / macOS

set HF_TOKEN="hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx" # Windows (cmd)
или

$env:HF_TOKEN="hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx" # Windows (PowerShell)
```


Если `HF_TOKEN` не установлен, `main.py` выбросит `RuntimeError("HF_TOKEN env var is not set")`.

---

## Как это работает

1. Скрипт ищет PDF-файлы в папке `data/input/` или в указанной пользователем папке.
2. Для каждого PDF:
   - Берётся **первая страница**.
   - PDF конвертируется в изображение через `pdf2image`.
   - Изображение ресайзится и сжимается (`image_to_base64` → JPEG + base64).
   - Собирается запрос к `https://router.huggingface.co/v1/chat/completions` с:
     - `SYSTEM_PROMPT` (подробная инструкция модели, что нужно извлечь и как оформить JSON);
     - `messages` с текстом и `data:image/jpeg;base64,...`.
   - Модель возвращает **строго JSON** (за счёт `response_format={"type": "json_object"}` и текста системного промпта).
   - JSON парсится в `dict`, затем нормализуется с помощью `REGEX_PATTERNS` (`normalize_with_patterns`).
   - На основе словаря создаётся строка таблицы (`create_row_from_extracted_data`).
3. После обработки всех файлов:
   - Формируется итоговый `DataFrame`.
   - Таблица сохраняется в Excel (`save_dataframe_to_excel`), путь выводится в консоль.
   - Печатается краткий отчёт (`print_brief_report`).

---

## Запуск

### Базовый сценарий

```
cd cadastr_parser
python main.py
```


Вы увидите меню:

======================================================================
🏛️ PDF PARSER ЕГРN - AI AGENT (Qwen3-VL)

📋 МЕНЮ:

    🚀 Обработать все PDF из папки по умолчанию

    📁 Обработать PDF из кастомной папки

    📊 Показать последний результат

    🧹 Очистить данные и начать заново

    ❌ Выход

Выберите нужный пункт, введя число от 1 до 5.

---

## Описание пунктов меню

1. **Обработать все PDF из папки по умолчанию**
   - Используется `INPUT_DIR` из `settings.py` (обычно `data/input/`).
   - Все найденные PDF обрабатываются с помощью Qwen3-VL.
   - Результат сохраняется в `data/output/output_cadastre_data.xlsx`.

2. **Обработать PDF из кастомной папки**
   - Можно указать любой путь к папке с PDF.
   - Поведение дальше аналогично пункту 1.

3. **Показать последний результат**
   - Открывает `data/output/output_cadastre_data.xlsx` (если существует).
   - Показывает:
     - путь к файлу;
     - размер;
     - количество строк и столбцов;
     - первые 5 строк таблицы.

4. **Очистить данные**
   - Удаляет текущий файл `output_cadastre_data.xlsx` из `data/output/` (после подтверждения).
   - Удобно, если нужно запустить обработку “с нуля”.

5. **Выход**
   - Завершение программы.

---

## Формат данных, которые извлекает модель

Модель Qwen3-VL по системному промпту должна возвращать JSON вида:

```
{
"cadastral_number": "74:36:0303005:454",
"address": "Челябинская область, г. Челябинск, ...",
"area": "13351",
"owner": "Левин Дмитрий Олегович",
"permitted_use": "(6.0) производственная деятельность ...",
"cadastral_cost": "13050468.99",
"land_category": "Земли населенных пунктов",
"rental_data": {
"rent_type": "Аренда",
"period_start": "02.09.2025",
"period_end": "31.12.2040",
"tenant": "ООО "УК ТЕХНОПАРК ЛД""
}
}
```


Даже если часть полей отсутствует, ключи должны присутствовать, а значения быть `null`.  
После этого значения дополнительно “чистятся” через `REGEX_PATTERNS` в `settings.py`.

---

## Логи

- Все логи пишутся через `logger_cfg.setup_logger`.
- Папка для логов задаётся в `settings.py` (`LOGS_DIR`, обычно `data/logs/`).
- В логах отражается:
  - старт/конец обработки;
  - ошибки API;
  - проблемы с PDF (невозможно прочитать/конвертировать);
  - статистика по файлам.

---

## Ограничения и рекомендации

- Qwen3-VL использует только **первую страницу** PDF. Если важные данные на других страницах — потребуется доработка.
- Ограничения Hugging Face Router по размеру:
  - Изображение сжимается до `max_size=(1024, 1024)` и JPEG `quality=80` для снижения размера.
  - При слишком тяжёлых PDF возможны ошибки `413 Payload Too Large` — в таком случае стоит дополнительно уменьшить DPI в `pdf_to_images`.
- Для корректной работы важно, чтобы макет выписок ЕГРН был более-менее типовым.

---

## Расширение

Идеи для будущих улучшений:

- Обработка всех страниц PDF, а не только первой.
- Поддержка нескольких разных типов документов (ЕГРН на здания, помещения, сервитуты и т.п.).
- Асинхронная обработка файлов.
- Веб-интерфейс или Telegram-бот поверх текущего CLI.

---

## Зависимости

```
# Основные зависимости
pandas>=2.0.0
openpyxl>=3.1.0
xlsxwriter>=3.0.0

# Логирование (используется стандартный logging, отдельного пакета не нужно)

# Работа с PDF → изображения
pdf2image>=1.16.0
Pillow>=10.0.0

# Работа с HTTP и Hugging Face Router
requests>=2.31.0

# Если используете типы и доп. утилиты (по желанию)
python-dateutil>=2.8.2

# Если где-то ещё используется pypdf / PyPDF2 (в старых модулях)
# pypdf>=4.0.0
# PyPDF2>=3.0.0
```
